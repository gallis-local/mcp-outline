apiVersion: v1
kind: Service
metadata:
  name: mcp-outline
  labels:
    app.kubernetes.io/name: mcp-outline
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-outline
  ports:
    - name: http
      port: 80
      targetPort: 3001
